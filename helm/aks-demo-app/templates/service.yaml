apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-demo-app.fullname" . }}
  labels:
    {{- include "aks-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
